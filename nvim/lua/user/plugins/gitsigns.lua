-- https://github.com/lewis6991/gitsigns.nvim
return {
  "lewis6991/gitsigns.nvim",
  opts = function(_, config)
    config = {
      -- signcolumn = false,
      -- numhl = true,
    }
    return config
  end,
}
